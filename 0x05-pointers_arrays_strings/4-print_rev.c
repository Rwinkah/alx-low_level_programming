#include "main.h"

/**
* print_rev - print a string in reverse
*
* @s: string pointer to be used
*
* Return: void
**/

void print_rev(char *s)
{
int j = 0;
while (*str != '\0')
{
str++;
j++
}

while (j > 0)
{
_putchar(*s);
s--;
j--
}

_putchar(10);
}
