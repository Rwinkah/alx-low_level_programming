#include "main.h"

/** 
* print_chessboard -  program to print every element of a 2d array
* 
 *@a: first array
* @b: sub array
*
* Return: void
**/



void print_chessboard(char board(*a)[8])
{
int i,j;
for (i = 0; i < a; i++)
{
for (j = 0; j < 8; j++)
)
_putchar(board[i][j]);
}
_putchar(10);
)
}
