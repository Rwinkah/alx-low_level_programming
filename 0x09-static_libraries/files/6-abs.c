#include "main.h"

/**
* _abs - entry point
*
* Description: "check for lowercase letters"
*
* @c: "The letter to check"
* Return: "success = 0"
**/

int _abs(int c)
{
if (c > 0)
return (c);
else
return (c * -1);
}
